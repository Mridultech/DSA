public class Basics {

    /*
     * hashing : In data structures used to create different data structures like map and set.
     * 
     * map and set : data strucutres in which time complexities of operations is very low and hence we have to use them widely.
     * 
     * types of maps : 
     * 1. hashmap : unordered map, O(1) time complexity for insertion, deletion and search.
     * 2. linkedhashmap : ordered map, O(1) time complexity for insertion, deletion and search.
     * 3. treemap : ordered map, O(logn) time complexity for insertion, deletion and search.
     * 
     * types of set :
     * 1. hashset : unordered set, O(1) time complexity for insertion, deletion and search.
     * 2. linkedhashset : ordered set, O(1) time complexity for insertion, deletion and search.
     * 3. treeset : ordered set, O(logn) time complexity for insertion, deletion and search.
     * 
     * 
     */
    public static void main(String[] args){

    }
}